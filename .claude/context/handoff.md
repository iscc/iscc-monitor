## 2026-06-21 — Adopt iscc-lib Go codec + bump the locked toolchain to Go 1.26 (ADR-0011)

**Done:** Bumped the locked toolchain Go 1.24 → 1.26 across all four config surfaces, added the
pinned `github.com/iscc/iscc-lib/packages/go` v0.5.0 dependency, and made it live with one tripwire
test in `internal/index` that asserts iscc-lib **currently rejects** the golden ISCC-IDv1
`"MAIGHFECJMOPMIAB"` with an "invalid Version" error — so when iscc/iscc-lib#43 lands the assertion
flips red and signals "migrate the in-repo port". The production decoder leaf (`iscc.go`) is
untouched and stays iscc-lib-free + WASM-buildable.

**Files changed:**
- `go.mod`: `go 1.24.0` → `go 1.26.1` directive (no `toolchain` line needed — gate green without
  one); added `require github.com/iscc/iscc-lib/packages/go v0.5.0`.
- `go.sum`: regenerated by `go mod tidy` — 3 new transitive deps land
  (`github.com/zeebo/blake3`, `golang.org/x/text`, `github.com/klauspost/cpuid/v2 // indirect`), all
  pure-Go / `CGO_ENABLED=0`-clean.
- `mise.toml`: `[tools]` `go = "1.24"` → `"1.26"` (single source of truth for the toolchain).
- `.github/workflows/ci.yml`: `setup-go` `go-version: "1.24"` → `"1.26"`.
- `.devcontainer/Dockerfile`: comment-only `(go = "1.24")` → `(go = "1.26")`; no build-logic change.
- `internal/index/iscclib_tripwire_test.go` (new): `TestISCCLibRejectsISCCIDv1Today` — imports
  iscc-lib, asserts `IsccDecode("MAIGHFECJMOPMIAB")` returns a non-nil error containing
  `"invalid Version"`; cites iscc/iscc-lib#43 + the migration-trigger instruction.

**Verification:** `mise install go@1.26` provisioned Go 1.26.4; `go version` → `go1.26.4`.
`mise run check` → GREEN under Go 1.26.4 (build + vet + test, all 21 packages `ok`); `gofmt -l .`
empty (excluding gitignored `cauldron/`). Per-criterion:
- [x] `go test -count=1 -run TestISCCLib ./internal/index` → PASS (asserts the Version-1 rejection
  today).
- [x] `go test -count=1 ./internal/index` → PASS (no regression of the existing decoder
  golden/mutation suite).
- [x] `GOOS=js GOARCH=wasm go build ./internal/index` → OK (production leaf stays WASM-pure).
- [x] `go.mod` has `go 1.26.1` + the iscc-lib require; `grep -c iscc-lib go.sum` = 2, with matching
  `zeebo/blake3` + `golang.org/x/text` + `klauspost/cpuid` lines.
- [x] `mise.toml` shows `go = "1.26"`; `ci.yml` shows `go-version: "1.26"`.
- [x] `grep -rn "iscc-lib" internal/index/iscc.go` empty (production leaf does not import iscc-lib).
- [x] Dependency boundary proven via `go list`: iscc-lib is in the **test** closure
  (`go list -test -deps`) but NOT in the non-test build closure (`go list -deps`) — the require is
  honest/live yet the WASM artifact stays clean.
- [x] Tripwire is non-vacuous (mutation): flipping `err == nil` → `err != nil` FAILS and prints the
  exact migration-trigger message; restored + tree clean.

**Next:** ADR-0011 is closed. The interim `internal/index.Decode` ISCC-IDv1 port stays in place by
the ADR carve-out; the tripwire now watches iscc/iscc-lib#43 for the migration trigger. The unblocked
M-UI tail resumes: the OTS store seam (unblocking certificate §5 BITCOIN ANCHOR + the `ots` bundle
member) or the WASM in-browser re-verifier (`monitor.iscc.codes`). Still-open `normal` issues remain
queued: the `host:port` `did:web:` mis-render on §4 + `serveBundle`'s `bundle.Hub.DID` (fix both DID
sites together, `%3A`-encode the port); the §6 `· at` per-record timestamp (needs an `iscc_index`
schema column); the Hub-List `ForceQuery` fail-open guard.

**Notes:**
- The toolchain bump and the dep add are atomic, as `next.md` required: under Go 1.24,
  `go mod download …iscc-lib…@v0.5.0` would try to switch toolchains (iscc-lib's `go.mod` declares
  `go 1.26.1`), so both landed in the same commit. CI's `actions/setup-go@v5` with `go-version: "1.26"`
  resolves the latest 1.26 patch (>= the 1.26.1 floor iscc-lib requires), so CI will satisfy the
  patch-pin caveat without an explicit `toolchain` line. **CI runs on the runner's checkout-fresh
  module cache** — the new deps will be downloaded there; they are pure-Go so no toolchain/cgo issue.
- Oracle/conformance gate: N/A. This step adds a dependency + a tripwire test and does NOT touch
  signature verification, RFC-6962/Merkle consistency, or proof code. No gate weakened — no `//nolint`,
  `t.Skip`, build-tag exclusion, or swallowed error.
- Scope: 5 config/source files modified + 1 new test file (within the ≤3 non-test-source budget — the
  three application-source surfaces are `go.mod`/`go.sum`/`mise.toml`; `ci.yml` + `Dockerfile` are
  CI/docs config; `iscc.go` untouched per the ADR carve-out).
- Asserted only the bare golden form `"MAIGHFECJMOPMIAB"` (safe, matches `iscc_test.go:35`). Did not
  add an `"ISCC:"`-prefixed sub-case — iscc-lib's `IsccDecode` strips the prefix before `decodeHeader`,
  so it would hit the same Version rejection, but the bare form is the load-bearing parity assertion
  and adding the prefixed case is YAGNI for the tripwire's single purpose.
