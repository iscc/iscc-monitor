## 2026-06-22 — Make `verify.wasm` reproducible — add `-buildvcs=false`, rebuild, re-pin `WasmVerifyHash`

**Done:** Added `-buildvcs=false` to the `mise run build:wasm` task so `go build` no longer stamps
`debug.ReadBuildInfo` VCS metadata (`vcs.revision`/`vcs.modified`/`+dirty`) into the wasm `data`
section, rebuilt `internal/web/verify.wasm` deterministically, and re-pinned `WasmVerifyHash` to the
emitted hash. The committed artifact is now byte-identical across clean / dirty / repeated builds and
the published hash regenerates from a clean checkout — closing the open `critical` reproducibility issue.

**Files changed:**
- `mise.toml`: `[tasks."build:wasm"]` `run` line now `go build -trimpath -ldflags=-buildid=
  -buildvcs=false -o internal/web/verify.wasm ./cmd/wasm` (added the missing third flag; `-trimpath`
  and `-ldflags=-buildid=` kept, still load-bearing).
- `internal/web/web.go`: re-pinned `WasmVerifyHash` const to
  `f03b9b8973e308be12dd7d0c210e612d0aa388e6e8a7cdce4ea8515cd823047e`; updated the const's doc comment
  to list `-buildvcs=false` and note it is load-bearing for reproducibility.
- `internal/web/verify.wasm`: regenerated by `mise run build:wasm` (no VCS stamp; 2,870,385 bytes,
  down from the prior `+dirty` 2,870,559).

**Verification:** `mise run check` → green (all 25 packages `ok`). Per-criterion:
- `go test -count=1 -run TestWasmVerifyHashPinned ./internal/web` → PASS against the rebuilt artifact
  (`sha256(verify.wasm) == WasmVerifyHash`).
- `go test -count=1 -run TestWasmVerify ./internal/web` → PASS (serve route / content-type /
  ETag / 304 / 405 unchanged).
- `strings internal/web/verify.wasm | grep -c 'vcs\.'` → `0` (was `3`: `vcs.revision=b2667f86…`,
  `vcs.time=…`, `vcs.modified=true`).
- Reproducibility across tree states: `mise run build:wasm` on the committed tree vs after touching an
  untracked file (dirty tree) → byte-identical `f03b9b89…` both times; repeated builds also stable.
- `gofmt -l .` (excluding gitignored `cauldron/`) → empty.

**Next:** Honor the human sequencing steer in `target.md` (Titusz, 2026-06-22): front-load **M-UI
design-parity** ahead of the WASM `<script>` caller — start with bringing `/` to its mockup's named
regions (claim-lookup hero as a no-JS `GET` form → `/inclusion/…`, every row a dossier link,
masthead/instance-identity chrome + anchor/checkpoint columns; the open `normal` `/`-realm-index issue
filed by Titusz). The WASM `<script>` caller (and the `cmd/wasm/main.go:39-40` `js.Value.Int()`
truncation fix that belongs to it) resumes after the parity pass, now safely on top of a reproducible pin.

**Notes:**
- The emitted hash `f03b9b89…` and size 2,870,385 match the reviewer's independent measurement
  exactly, under Go 1.26.1 — the toolchain this loop runs.
- `git diff --stat internal/web/verify.wasm` shows the committed (dirty-stamped, 2,870,559) → rebuilt
  (clean, 2,870,385) byte delta; that is the intended change being committed, not a non-determinism
  signal. After this commit a fresh `mise run build:wasm` on the committed tree leaves the working tree
  clean (no diff).
- Scope held to the 3 files `next.md` named (`mise.toml`, `web.go`, generated `verify.wasm`). No
  `<script>` caller wired, no `cmd/wasm` source touched, no `proof/verify`/`verifyadapter` change, no
  new CI/`check` gate added. The existing `TestWasmVerifyHashPinned` guards the committed bytes against
  the const; per `next.md` no rebuild-and-compare was added to `check` (keeps it fast + linux-only).
- The `learnings/web.md` rule (lines 55–65) already states `-buildvcs=false` is required for any
  hash-pinned committed wasm/binary — this step is its canonical application.
