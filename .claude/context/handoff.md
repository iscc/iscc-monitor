# Handoff

_(Seed — overwritten by `advance` then `review` each iteration.)_

**First iteration:** no prior handoff. `define-next` should start from `target.md` M1 — bootstrap the
Go module and the first golden-tested pure unit (`origin()` / `verifierKey()`). There is no code and
no commits yet.
