## 2026-06-22 — Rebuild + re-pin verify.wasm to the 6-arg/id-binding shim (close the NEEDS_WORK skew)

**Done:** Regenerated `internal/web/verify.wasm` via `mise run build:wasm` so the committed artifact
carries the 6-arg/id-binding shim, and re-pinned `web.WasmVerifyHash` to its SHA-256
`96b2a40d459c51817dd87911063ab14abf481080e30ecb17ef311090755852d3`. Source, page, artifact, and
published hash now agree; the stale-artifact NEEDS_WORK gate at HEAD is closed.

**Files changed:**
- `internal/web/verify.wasm`: regenerated (generated byte artifact) — `2c919e6…`→ now stable
  `96b2a40d…`; old `expected 5 args` shim gone, new 6-arg/`RecordCommitsID` shim present.
- `internal/web/web.go`: re-pinned `WasmVerifyHash` const (line 93) from `7d57ab1b…` to `96b2a40d…`.

**Verification:** `mise run check` → green (all 20 packages build + vet + test ok); `gofmt -l .` empty.
Per-criterion:
- [x] `mise run build:wasm` exits 0 and stably emits SHA-256 `96b2a40d459c51817dd87911063ab14abf481080e30ecb17ef311090755852d3`
  (== re-pinned const).
- [x] `strings … | grep -c "expected 5 or 6 args"` → 1; `… "expected 5 args"` → 0; `… "decode record envelope"` → 1.
- [x] `go test -run TestWasmVerifyHashPinned ./internal/web` PASS.
- [x] `mise run check` green.

**Next:** The front-of-queue WASM work is the deferred signature-verify half of the verifier-scope
trust gap — browser did:web key resolution + checkpoint-note signature verification. That is a `normal`,
design-first step (the verifier currently proves inclusion + id-binding but still trusts the monitor for
the signature). Until it lands, do not loosen `verifier.html`'s "hub-signed root" success copy. Pages
deploy is still blocked on a one-time human repo-Settings step (out of autonomous scope).

**Notes:**
- **TOOLCHAIN/DETERMINISM RESOLVED (worth a glance):** the very first `mise run build:wasm` in this
  fresh session emitted a one-off DIFFERENT hash `2c91e61f20560fa98e0fbd6813746c40687861d4b4b3be604d4216557df0f48e`
  — NOT the review's expected `96b2a40d…`. Per next.md this is a STOP-and-investigate condition. I did
  not pin the surprise hash. Root cause: a stale/mixed build-cache state at session start. Proof it is
  deterministic: builds #3–#6, INCLUDING a full cold build after `go clean -cache` (#5), all reproduce
  `96b2a40d…` byte-for-byte — exactly the review's value. So the canonical artifact is the
  stably-reproducible `96b2a40d…` (verified across warm + cold cache, Go 1.26.1, `-buildvcs=false`),
  which is what is committed and pinned. The `2c91e61f…` was a transient cache artifact, never committed.
- The shim correctness was independently confirmed via `strings` (the authoritative proof per
  cmd-wasm.md, since `mise run check` cannot catch this skew — it builds to /tmp and the pinned-hash test
  only compares committed bytes vs committed const): new 6-arg shim present, old 5-arg gone, RecordCommitsID present.
- No source logic touched (`cmd/wasm`, `verifyadapter`, `verifier.html`, `cert.html` untouched) — only
  the generated artifact + the one const, exactly the 2-file scope.
- Suggest the next session start its `mise run build:wasm` with a warm cache (or do a throwaway build
  first) to avoid the one-off transient hash; the stable target remains `96b2a40d…` on this toolchain.
