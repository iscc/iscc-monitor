## 2026-06-22 — Review of: Build the verifier `.wasm` reproducibly (`mise run build:wasm`) and serve it byte-pinned at `/_ds/verify.wasm`

**Verdict:** NEEDS_WORK
**Loop:** CONTINUE

**Summary:** The SERVE half is correct: `GET /_ds/verify.wasm` is wired into the `/_ds/` leaf as
`application/wasm` with the same no-cache + strong-ETag + 304 + GET-only policy, `mise run check` is
green, scope is clean (3 non-test/doc files), and the served-asset + hash-pin tests are non-vacuous
(mutating the const → FAIL). But the REPRODUCIBLE-BUILD half — the entire point of the step — is broken:
the `mise run build:wasm` task omits `-buildvcs=false`, so `go build` stamps VCS metadata into the wasm,
and the committed artifact's pinned `WasmVerifyHash` (`17b0f4f8…`) **cannot be regenerated by the
documented task** (it was built from a dirty tree at the PARENT revision). This defeats the step's
core Verify/`Done When` contract.

**Verification:**
- [x] `mise run check` green — all 25 packages `ok`.
- [x] `gofmt -l .` empty (outside gitignored `cauldron/`).
- [x] `go test -run TestWasmVerify ./internal/web` → PASS (served 200 / `application/wasm` / no-cache /
  strong quoted ETag / body == embedded bytes / `\0asm` magic; hash-pin holds and is mutation-proven —
  flipping one hex char of the const FAILS `TestWasmVerifyHashPinned`).
- [x] `go test -run 'TestIfNoneMatch304|TestMethodNotAllowed' ./internal/web` → PASS with `WasmVerifyPath`
  in both lists (If-None-Match → 304; POST → 405).
- [x] Committed `verify.wasm` first 4 bytes are the Wasm magic `00 61 73 6d`; size 2,870,559.
- [x] go.mod/go.sum byte-identical (no new dep); WASM gate `GOOS=js GOARCH=wasm go build ./cmd/wasm` OK;
  scope = exactly the 3 non-test/doc files next.md named (`mise.toml`, `web.go`, generated `verify.wasm`).
- [x] Gate-circumvention scan over `origin/develop..HEAD` — no `//nolint`/`t.Skip`/build-tag/deleted-assertion.
- [ ] **`mise run build:wasm` re-run after `go clean -cache` is byte-identical (reproducibility holds)** —
  **FAIL.** The build is NON-deterministic and unreproducible. Measured 10× via the documented task:
  `d99e9a4f…` ×9 (dirty tree) and `2f3dc3eb…` ×1 (clean tree); the pinned `17b0f4f8…` reproduced **0×**.
  Root cause (Codex P1, reviewer-confirmed): the task lacks `-buildvcs=false`, so Go embeds VCS metadata.
  `strings internal/web/verify.wasm | grep vcs.` on the committed blob shows `vcs.revision=b2667f86215c…`
  (the PARENT of the advance commit), `vcs.modified=true`, `mod …+dirty`. HEAD has since moved, so the
  task now stamps a different revision → a different `data` section (an 8-byte size delta, clean vs dirty)
  → a different SHA-256 every regeneration. `TestWasmVerifyHashPinned` stays green only because it never
  rebuilds — it compares the const to the COMMITTED bytes. A clean-checkout / CI rebuild-and-compare can
  never pass.
- [ ] **`Done When`: deterministic `mise run build:wasm` + pinned SHA-256** — **FAIL** (same root cause).
- Oracle/conformance gate: N/A — static-asset transport; no signature/RFC-6962/Merkle/did:web/proof/fsck
  code touched.

**Issues found:** ONE `critical` (filed) — "`verify.wasm` is NOT reproducible — committed bytes embed a
`+dirty` parent-revision VCS stamp; the published hash cannot be regenerated". Fix: add `-buildvcs=false`
to the `build:wasm` task, rebuild, re-pin `WasmVerifyHash`. Reviewer-verified that `-buildvcs=false`
yields a STABLE single hash `f03b9b89…` (2,870,385 bytes) byte-identical across clean / dirty /
`go clean -cache`. Blocks PASS because it negates the step's stated deliverable; `critical` because the
next iteration must re-do the artifact before the loop builds the `<script>` caller on top of an
unreproducible pin.

**Codex second opinion:** ONE [P1] — "Disable VCS stamping in the WASM build" (`mise.toml:42`). Codex
independently found the exact root cause: the committed blob was built as the parent revision
(`b2667…+dirty`) while the task now emits bytes for a different revision (and dirty bytes on later runs),
so the regenerated SHA-256 differs and `TestWasmVerifyHashPinned` fails after the prescribed rebuild; the
fix is `-buildvcs=false` (then rebuild + re-pin). **CONFIRMED real** — reviewer reproduced it
quantitatively (10-build hash tally; `strings … grep vcs.` on the committed blob; `-buildvcs=false`
stability check) and filed it as the `critical` issue above. Codex exited 0, no other findings.

**Visual check:** n/a — no SSR surface changed. The only Go change is `internal/web/web.go` (a static
`.wasm` asset route + consts); no `internal/dashboard|dossier|web|certificate` template or rendered HTML
was touched. `verify.wasm` is a binary artifact, not a rendered surface.

**Next:** This step must be re-done first: add `-buildvcs=false` to `mise.toml`'s `build:wasm` task,
`mise run build:wasm`, re-pin `WasmVerifyHash` to the emitted hash, and re-commit the regenerated
`verify.wasm` — then confirm two builds from different tree states are byte-identical and
`TestWasmVerifyHashPinned` passes against the rebuild. **AFTER that**, honor the human sequencing steer
landed in `target.md` (Titusz, 2026-06-22): front-load **M-UI design-parity** ahead of the WASM `<script>`
caller — (1) bring `/` to its mockup's named regions (claim-lookup hero as a no-JS `GET` form →
`/inclusion/…`, every row a dossier link, masthead/instance-identity chrome + anchor/checkpoint columns;
new `normal` issue filed by Titusz), (2) the same named-region + `←` back-link parity pass across dossier
/ log browser / record / certificate, (3) then resume the WASM `<script>` caller → OTS.

**Notes:**
- The advance's handoff claimed "re-run after `go clean -cache` byte-identical … reproducibility holds
  under the project toolchain". That claim is FALSE as written — but the underlying observation was real:
  `go clean -cache` rebuilds ARE byte-identical *to each other when the tree state is held constant*.
  The advance verified within one session/tree-state and missed that the VCS stamp varies the output
  across commit/dirtiness. The build is reproducible ONLY after `-buildvcs=false` removes the stamp.
- `-trimpath -ldflags=-buildid=` are still correct and load-bearing (they kill GOROOT/module-path and
  buildid nondeterminism); they are just insufficient on their own — `-buildvcs=false` is the missing
  third flag for any committed-and-hash-pinned artifact. Recorded in `learnings/web.md`.
- Uncommitted human edits were present at review start and LEFT UNTOUCHED: a sequencing steer in
  `target.md` and a new `normal` `/`-realm-index issue in `issues.md` (both `[human] Titusz` 2026-06-22).
  I did not modify or revert `target.md` (protocol forbids it) and preserved the human issue.
- No `critical` was open before this review; this one blocks PASS but the Loop is CONTINUE (the next
  iteration re-does the artifact). Not DONE — open critical + normals remain; not STOP — the fix is
  unambiguous and low-risk (a one-flag task change + regenerate + re-pin), no human-only decision.
- NEEDS_WORK → not pushed; the next cycle fixes the build flag first.
