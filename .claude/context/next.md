# Next Work Package

## Step: Rebuild + re-pin verify.wasm to the 6-arg/id-binding shim (close the NEEDS_WORK skew)

## Advances
This is the explicit `review` handoff (`f5e8f59`, verdict **NEEDS_WORK / loop CONTINUE**) and the
top-of-state instruction: *"Close the WASM stale-artifact skew first — it is the NEEDS_WORK gate at
HEAD."* It completes the user-visible goal of the prior increment (the browser gates `verified` on
id-binding), which advances the **WASM verifier upgrade** milestone Verify criterion:

> "identical vectors yield identical verdicts (WASM vs server); the verifier artifact hash matches the
> published value"

The committed source (`verifier.html:628` passes a 6th `id` arg; `cmd/wasm` shim accepts 5-or-6 args
and gates `verified` on `verifyadapter.RecordCommitsID`) contradicts the committed byte artifact
`internal/web/verify.wasm`, which still carries the old 5-arg shim — so the artifact does NOT match what
the source produces, and the deployed verifier would render `error` for every target. This preempts all
other milestone work because it is the open NEEDS_WORK gate: the increment cannot be PASSed and pushed
until the source and the pinned artifact agree.

## Goal
Regenerate the pinned `internal/web/verify.wasm` from the current `cmd/wasm` source (so the shipped
module carries the 6-arg/id-binding shim) and re-pin `web.WasmVerifyHash` to its SHA-256, making the
committed tree internally consistent: source, page, artifact, and published hash all agree.

## Scope
- **Create**: (none)
- **Modify**:
  - `internal/web/verify.wasm` — regenerate via `mise run build:wasm` (a generated byte artifact, never
    hand-edited).
  - `internal/web/web.go` — re-pin the `WasmVerifyHash` const (line 93) to the emitted SHA-256.
- **Reference**:
  - `.claude/context/learnings/cmd-wasm.md` — the "TRAP — the committed verify.wasm is a PINNED BYTE
    ARTIFACT, not auto-rebuilt" rule, the deterministic build flags, and the strings-grep verification.
  - `internal/web/web.go:84-93` + `:156-163` — the `WasmVerifyHash` const doc and the `//go:embed
    verify.wasm` declaration (the reproducible-build contract).
  - `internal/web/web_test.go:288-298` — `TestWasmVerifyHashPinned`, the gate that ties the committed
    bytes to the const.
  - `mise.toml` `[tasks."build:wasm"]` — the exact deterministic build command
    (`GOOS=js GOARCH=wasm CGO_ENABLED=0 go build -trimpath -ldflags=-buildid= -buildvcs=false`).

## Not In Scope
- **Do NOT change any `cmd/wasm`, `cmd/wasm/verifyadapter`, `verifier.html`, or `cert.html` logic.**
  The source is correct and mutation-proven; this step only rebuilds the artifact to match it. Editing
  the shim again would reopen the review's PASS reasoning on the id-binding work.
- The **signature half** of the verifier-scope trust gap (browser did:web resolution + checkpoint-note
  signature verify) — a separate open `normal`, design-first, a later step.
- Wiring a tier-2 WASM island into the **hub dossier** — later WASM sub-step.
- Unblocking the **Pages deploy** (`Configure Pages` failure) — needs a one-time human repo-Settings
  step, not code; out of this autonomous step.
- Do NOT hand-edit `verify.wasm`, and do NOT relax `TestWasmVerifyHashPinned`, the `-buildvcs=false`
  flag, or any gate to make the hash match.

## Implementation Notes
- **Run the canonical task, not a bare `go build`.** Use `mise run build:wasm` (it sets
  `GOOS=js GOARCH=wasm CGO_ENABLED=0` and the deterministic flags `-trimpath -ldflags=-buildid=
  -buildvcs=false -o internal/web/verify.wasm ./cmd/wasm`). The `-buildvcs=false` is load-bearing per
  the `web.go` doc — without it Go stamps VCS revision/dirty metadata into the wasm data section and the
  hash is unreproducible. Under the project-pinned Go 1.26.1 toolchain (confirmed present).
- **Re-pin from the freshly emitted bytes.** Compute `sha256sum internal/web/verify.wasm` (lowercase
  hex) and set `WasmVerifyHash` in `internal/web/web.go:93` to that value, replacing the stale
  `7d57ab1b…`. The `review` independently computed the expected value
  `96b2a40d459c51817dd87911063ab14abf481080e30ecb17ef311090755852d3` — confirm the local rebuild emits
  exactly that; if it differs, STOP and report the toolchain/flag discrepancy rather than pinning a
  surprise hash (a divergence means the build was not deterministic and the cause must be found first).
- **Correctness rule (from `cmd-wasm.md`, load-bearing):** `mise run check` CANNOT catch this skew — it
  builds `cmd/wasm` to `/tmp`, and `TestWasmVerifyHashPinned` only checks the committed bytes match the
  committed const (both stale → green). The authoritative proof the rebuild took effect is the `strings`
  grep for the new shim message, not a green build. Verify the artifact actually carries the new shim
  with the grep in the Verification section below.
- Keep the `web.go` doc comment around `WasmVerifyHash` accurate — it already states the hash tracks
  `mise run build:wasm`; only the const value changes, the surrounding prose stays correct.

## Verification
- `mise run build:wasm` exits 0 and the rebuild emits SHA-256
  `96b2a40d459c51817dd87911063ab14abf481080e30ecb17ef311090755852d3` (i.e.
  `sha256sum internal/web/verify.wasm` equals the re-pinned `WasmVerifyHash`).
- `strings internal/web/verify.wasm | grep -c "expected 5 or 6 args"` → `1` (the new shim) AND
  `strings internal/web/verify.wasm | grep -c "expected 5 args"` → `0` (old shim gone) AND
  `strings internal/web/verify.wasm | grep -c "decode record envelope"` → `1` (RecordCommitsID present).
- `go test -run TestWasmVerifyHashPinned ./internal/web` passes against the re-pinned const.
- `mise run check` is green (`go build ./...`, `go vet ./...`, `go test ./...`; `gofmt -l .` empty).

## Done When
`mise run build:wasm` reproduces the pinned `internal/web/verify.wasm` (its SHA-256 ==
`web.WasmVerifyHash`), the artifact's `strings` confirm the 6-arg/id-binding shim, and `mise run check`
(including `TestWasmVerifyHashPinned`) is green — so the committed source, page, artifact, and published
hash are internally consistent.
